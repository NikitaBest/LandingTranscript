# Landing Transcript - Лендинг для AI-транскриптора

Современный лендинг для AI-сервиса транскрибации консультаций стоматологов, построенный на React + Express + TypeScript.

## 🚀 Технологический стек

- **Frontend**: React 19, TypeScript, Vite, Tailwind CSS
- **Backend**: Express.js, TypeScript
- **База данных**: PostgreSQL (Drizzle ORM)
- **UI компоненты**: Radix UI, shadcn/ui
- **Роутинг**: Wouter
- **Сборка**: Vite + esbuild

## 📋 Требования

- **Node.js**: версия 18.x или выше (рекомендуется 20.x)
- **PostgreSQL**: версия 12 или выше
- **npm** или **yarn** или **pnpm**

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd LandingTranscript
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваши настройки:

```env
NODE_ENV=development
PORT=3000
HOST=localhost
DATABASE_URL=postgresql://user:password@localhost:5432/landing_transcript
```

### 4. Настройка базы данных

Убедитесь, что PostgreSQL запущен и создайте базу данных:

```bash
createdb landing_transcript
```

Или через psql:

```sql
CREATE DATABASE landing_transcript;
```

Затем примените миграции схемы:

```bash
npm run db:push
```

### 5. Запуск в режиме разработки

Запустите сервер разработки (клиент и сервер вместе):

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

Клиент с hot-reload будет доступен на: `http://localhost:5173` (если запущен отдельно)

### 6. Запуск только клиента (для разработки UI)

```bash
npm run dev:client
```

Клиент будет доступен на: `http://localhost:5173`

## 📜 Скрипты package.json

| Скрипт | Описание |
|--------|----------|
| `npm run dev` | Запускает сервер в режиме разработки (клиент + сервер) |
| `npm run dev:client` | Запускает только клиент с Vite dev server на порту 5173 |
| `npm run build` | Собирает проект для продакшена (клиент + сервер) |
| `npm run start` | Запускает собранное приложение в продакшен режиме |
| `npm run check` | Проверяет TypeScript типы без сборки |
| `npm run db:push` | Применяет изменения схемы БД к базе данных |

## 🌍 Переменные окружения

### Обязательные переменные

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://user:pass@localhost:5432/dbname` |

### Опциональные переменные

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `NODE_ENV` | Режим работы (`development` \| `production`) | `development` |
| `PORT` | Порт для запуска сервера | `3000` |
| `HOST` | Хост для запуска сервера | `localhost` |

### Переменные для Replit (опционально)

Эти переменные нужны только при деплое на Replit:

- `REPL_ID` - ID Replit проекта
- `REPLIT_INTERNAL_APP_DOMAIN` - Внутренний домен приложения
- `REPLIT_DEV_DOMAIN` - Домен для разработки

Подробнее см. `.env.example`

## 🏗️ Структура проекта

```
LandingTranscript/
├── client/                 # Клиентское приложение (React)
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   │   ├── landing/    # Компоненты лендинга
│   │   │   └── ui/         # UI компоненты (shadcn/ui)
│   │   ├── pages/          # Страницы приложения
│   │   ├── hooks/          # React хуки
│   │   └── lib/            # Утилиты
│   ├── public/             # Статические файлы
│   └── index.html          # HTML шаблон
├── server/                 # Серверное приложение (Express)
│   ├── index.ts            # Точка входа сервера
│   ├── routes.ts           # API роуты
│   ├── storage.ts          # Слой работы с БД
│   └── static.ts           # Статические файлы
├── shared/                 # Общий код (клиент + сервер)
│   └── schema.ts           # Схема БД (Drizzle)
├── api/                    # Vercel serverless функции
│   └── index.ts            # Express app для Vercel
├── dist/                   # Собранные файлы (не в git)
│   ├── public/             # Собранный клиент
│   └── index.cjs           # Собранный сервер
├── script/                 # Скрипты сборки
│   └── build.ts            # Скрипт сборки
├── vite.config.ts          # Конфигурация Vite
├── drizzle.config.ts       # Конфигурация Drizzle ORM
├── vercel.json             # Конфигурация Vercel
└── package.json            # Зависимости и скрипты
```

## 🚢 Деплой на Vercel

### Подготовка

1. Убедитесь, что проект собран локально без ошибок:
   ```bash
   npm run build
   ```

2. Проверьте, что все переменные окружения указаны в `.env.example`

### Деплой через Vercel CLI

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Войдите в Vercel:
   ```bash
   vercel login
   ```

3. Деплой:
   ```bash
   vercel
   ```

4. Для продакшен деплоя:
   ```bash
   vercel --prod
   ```

### Деплой через GitHub

1. Подключите репозиторий к Vercel через веб-интерфейс
2. Vercel автоматически определит настройки из `vercel.json`
3. Добавьте переменные окружения в настройках проекта Vercel:
   - `DATABASE_URL` - URL вашей PostgreSQL базы данных
   - `NODE_ENV=production`
   - `PORT` - Vercel установит автоматически

### Настройка базы данных на Vercel

1. Создайте Postgres базу данных в Vercel:
   - Перейдите в Dashboard → Storage → Create Database → Postgres
   - Скопируйте `DATABASE_URL` из настроек базы данных

2. Добавьте `DATABASE_URL` в Environment Variables проекта

3. Примените миграции (можно через Vercel CLI или вручную):
   ```bash
   vercel env pull .env.local
   npm run db:push
   ```

### Важные замечания для Vercel

- Vercel автоматически устанавливает переменную `PORT` - не указывайте её вручную
- Не указывайте `HOST` в переменных окружения на Vercel
- Убедитесь, что `NODE_ENV=production` установлен (устанавливается автоматически через `vercel.json`)
- Файл `vercel.json` настроен для правильной работы с этой структурой проекта
- Проект использует `api/index.ts` как serverless функцию для Express приложения
- Статические файлы (клиент) раздаются из `dist/public/`
- Все запросы (включая API) проходят через `/api` endpoint

## 🔍 Проверка типов

Проверка TypeScript типов без сборки:

```bash
npm run check
```

## 📦 Сборка для продакшена

Сборка создает оптимизированные файлы в директории `dist/`:

```bash
npm run build
```

Результат:
- `dist/public/` - собранный клиент (React приложение)
- `dist/index.cjs` - собранный сервер (Express приложение)

## 🏃 Запуск продакшен версии

После сборки:

```bash
npm run start
```

Приложение будет доступно на порту, указанном в `PORT` (по умолчанию 3000).

## 🛠️ Разработка

### Добавление новых компонентов

Компоненты лендинга находятся в `client/src/components/landing/`

UI компоненты (shadcn/ui) находятся в `client/src/components/ui/`

### Добавление API роутов

API роуты добавляются в `server/routes.ts`. Все роуты должны иметь префикс `/api`

### Работа с базой данных

Схема БД определяется в `shared/schema.ts` с помощью Drizzle ORM.

Для применения изменений схемы:

```bash
npm run db:push
```

## 🐛 Решение проблем

### Проблема: "Could not find the build directory"

**Решение**: Убедитесь, что вы выполнили `npm run build` перед запуском продакшен версии.

### Проблема: "DATABASE_URL is not defined"

**Решение**: Убедитесь, что файл `.env` существует и содержит `DATABASE_URL`.

### Проблема: Ошибки подключения к БД

**Решение**: 
- Проверьте, что PostgreSQL запущен
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что база данных создана

### Проблема: Порт уже занят

**Решение**: Измените `PORT` в `.env` на другой порт (например, 3001).

## 📝 Лицензия

MIT

## 👥 Авторы

Разработано для Odonta AI

---

**Примечание**: Для локальной разработки убедитесь, что PostgreSQL запущен и доступен. Для продакшена на Vercel используйте Vercel Postgres или внешний PostgreSQL хостинг.
